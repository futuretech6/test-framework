{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make binary",
            "type": "shell",
            "command": "make",
            "args": [
                "bin/${fileBasenameNoExtension}.out"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
